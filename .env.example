# Environment Configuration
NODE_ENV=production

# Server Configuration
PORT=3000

# Database Configuration
# Replace with your MongoDB Atlas connection string or local MongoDB URL
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/phonewraps?retryWrites=true&w=majority

# JWT Configuration
# CRITICAL: Generate a strong random secret (min 32 chars, recommended 64+ chars)
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-to-random-64-char-string

# Cloudinary Configuration (Required for image uploads)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Razorpay Payment Gateway (Optional - required for payments)
RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-key-secret

# Twilio SMS/OTP Service (Optional - required for SMS OTP)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=your-twilio-phone-number

# CORS Configuration (Optional - defaults to localhost:3000)
CORS_ORIGIN=https://your-frontend-domain.com

# Email Service Configuration (Optional - for email OTP/notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-email-password

# Production Security Notes:
# 1. NEVER commit the actual .env file to version control
# 2. Use strong, randomly generated secrets for JWT_SECRET
# 3. Rotate secrets periodically
# 4. Use environment variables in your hosting platform (Vercel, Heroku, AWS, etc.)
# 5. Keep API keys secure and never expose them in client-side code
